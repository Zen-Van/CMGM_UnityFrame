---
description: 用户协作偏好（注释、教学导向、改动纪律）
alwaysApply: true
---

# 用户协作偏好

## 注释 — 严禁删除

- **不要删除已有注释**（含 `///` XML 文档、`//` 行内与块注释、`Tooltip`、`Header`、TODO、教学说明）。
- 重构、简化、回退、格式化时：**保留原注释**；若逻辑变了，**改注释内容**，而不是删掉。
- 新增代码时：对非自明的业务意图、竖切里程碑（如 `#5`、`#6`）、过渡说明，**延续项目现有注释风格**补全。
- 仅当注释与代码**明显矛盾且已过时**时，才更新或替换该条注释；**不要**以「代码已自解释」为由整段移除。

## 教学为主

- 优先 **少文件、短调用链、能一眼读懂** 的实现；避免为「可扩展」提前引入 Registry / 工厂等抽象，除非用户明确要求。
- 大改前先对齐范围；竖切步骤（如 Loading 1.4）**先讨论再写**，不堆用户未验收的架构。

## 其它

- 回复使用**简体中文**。
- 未明确要求时**不要** git commit / push。
- 只做任务范围内的最小 diff；不顺手改无关文件。
