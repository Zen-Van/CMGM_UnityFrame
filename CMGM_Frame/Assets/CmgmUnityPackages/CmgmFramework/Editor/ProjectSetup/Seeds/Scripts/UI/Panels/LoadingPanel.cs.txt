using CMGM.UI;

namespace CMGM.Workspace
{
    /// <summary>
    /// 加载进度面板（HotRes/UI/Panels/LoadingPanel.prefab）。
    /// <para>控件名约定：<c>sldProgress</c>、<c>txtStatus</c>（由 LoadingManager 经 GetControl 驱动）。</para>
    /// </summary>
    public class LoadingPanel : BasePanel
    {
    }
}
