---
description: 教学导向、路线图命名、每步说明与「下一步」总表格式
alwaysApply: true
---

# 教学导向与路线图协作

## 核心原则

- 总目标：迭代框架时**教会用户**（授人以渔）；用户会说明懂/不懂，不懂处讲细。
- **代码正确性略高于迁就理解**；冲突时优先架构/质量，再用更细讲解补齐。
- **分步迭代**：先做能用的最小版本，再完善；不一次写到尽善尽美。
- 大改前先对齐范围；竖切步骤**先讨论再写**，不堆未验收的架构。

## 每步完成后的默认说明（教学向）

每做完路线图里的一步，**默认**用教学性说明（不是只列改动清单，**也不要自动和上一步对比**）：

- **这一步的目标**（对应哪条主线/支线、哪一步编号）
- **实际改了什么**（文件 / 类型 / 依赖）
- **为什么这样改**（设计取舍、没走别的路的原因）
- **你需要知道的一个要点**（如 asmdef 边界、调用链）

## 对比总结（仅用户明确要求时）

用户说「总结这一步改了什么」「和上一步对比」等时再做：

- 基准 = **上一步已提交 / 已验收**状态
- 列：相对基准**新增 / 变更 / 未动 / 仍待做**
- 给一条**推荐 git 提交备注**（对齐仓库近期 commit 风格）
- 平时做完一步**不要自动对比**

## 任务命名

- 用**汉字全称 + 步骤编号**：如「Loading系统1.1」「Editor测试系统1.2」
- **主线任务 / 支线任务**（不用 L1/A2 等字母缩写）
- 主线步骤用**功能性章节名 + 连续编号**（如「启动编排3.4」「Loading系统1.2」），未做部分编号连续无缺口

## 「下一步」报告格式

用户问下一步、或一步做完时，列**一张总表**（所有主线 + 支线，含未解锁）：

- **未解锁**：标注解锁条件（如「Loading 竖切 closure 后」）
- **可做项**：步骤编号 + 预计变更量 + **教学难度**（1~5 或 低/中/高）
- **每条线只列最前节点一条**（线性推进）；用户**主动问某条线后续**时，再列该线完整步骤表

## 框架 vs 业务边界

- **框架**（`CmgmFramework`）：机制、启动链、通用 Task；**不**配玩法场景名、新建/读档分支
- **业务**（`_WorkSpace`）：进游戏、读档、场景名、玩法 UI；等产品定稿后在业务 State / LoadTasks 实现
- **不做 LoadingProfile SO**（除非用户日后明确要求）；清单在代码 `CreateTasks()`
- **Editor 测试入口（定案）：** Main Toolbar Overlay + 自带 Play 劫持；见 ARCHITECTURE **§4.3.1**

## 其它

- **先标路线图再答**：涉及后续会改的内容，先写在哪一步迭代
- **框架未发布**：优先删盘/重建，不默认迁移代码；用户告知「已发布」后再谈兼容
- **ShowPanel 泛型优先**（见 ARCHITECTURE §8）
- 注释保留规则见 `user-preferences.mdc`
